## TODO

### Render
- [ ] Сделать прослойку между Render и Backend API (OpenGL)
- [x] ~~Добавить статистику по рендеру: draw calls, count vertex, indices, triangles~~
- [x] ~~Вынести Transform из Renderer в отдельную сущность (файл)~~
- [ ] Пофиксить баг рендера источника света с моделью (модель не освещается)
- [ ] Вынести из рендера захардкоженные примитивы (куб, пирамида)

### GUI 
- [x] Загрузка настроек из ini-файла в ImGui при старте;

### Editor
- [ ] Реализовать панель ассетов с отображением возможных ассетов (модель, примитивы или источник света) 
      - [x] Реализовать добавление ассета с панели ассета на сцену 
            - [ ] dragNdrop ?
            - [x] добавление модели по клику на название модели
- [x] Вынести функционал Gui из ядра движка в сущность editor
- [ ] Добавить камеру в Hierarchy panel
- [x] Inspector panel. Вывести сведения об выбранных объектах
- [x] Inspector panel. Выбор и привязка текстур (diffuse map и specular map) к кубам (примитивы)
- [ ] Inspector panel. Если на сцену добавлены две одинаковые модели (имена одинаковые), то выбирается только первая добавленная на сцену.
- [ ] Hierarchy panel. Убрать дубли кода из метода отрисовки иерархии сцены

### Resource Manager 
- [x] Принимать на вход путь (абсолютный или относительный)
- [x] Определять абсолютный путь до ресурса
- [x] Загружать ресурс (шейдер, текстура) и по запросу отдавать подсистемам

### Logger
- [ ] Добавить таймстампы для сообщений
- [ ] Цветовая индикация для разных уровней сообщений логгера
- [ ] Передача множественных аргументов для печати в более удобном виде
- [ ] Добавить опцию отключение вывода лог в консоль при помощи option CMake

### Shader
- [ ] Реализовать простой парсер файлов шейдеров на уровне include 
- [ ] Вынести функционал OpenGL по созданию, присоединению и линковки шейдеров в отдельные методы класс
- [ ] Добавить метод Sampler2D для текстур

### Light subsystem
- [ ] Сейчас позиция и направление для DirLight одно и тоже.
- [ ] **Баг** Spot light не работает, если является единственным источником света на сцене

### Scene subsystem
- [ ] Реализовать сохранение сцены в JSON
- [ ] Реализовать загрузку сцены из JSON
- [ ] Кэшировать загруженные модели. Если пользователь загружает одну и ту же модель, то грузить модель из кэша (копирование существующей модели) чтобы снизить дергание Asimp::Importer IO

### Разное
- [ ] Заменить сырые указатели на умные указатели
- [ ] Пофиксить warnings компилятора
- [ ] Добавить юнит-тесты (библиотека catch2)
- [ ] Свой класс строк. Чтобы явная конверсия в const char*. А так же возможность задания размера SSO при создании строки.\
- [ ] Разобраться с подключением hpp файлов через #include, чтобы избавиться от конструкций вида "#include "../../../target"
 
### Assimp 
- [x] На текущей конфигурации ОС (archlinux x64) не собирался проект. Ругался на отсутствие unzip.h. Пришлось ручками удалить ветку в if-блоке, где подключался unzip.h в Assimp. И теперь подключается #include <minizip/unzip.h>

### Texture
- [ ] Метод GetId() возвращает ссылку на внутреннее поле (m_id), что не правильно. Может сделать отдельный метод set() или id публичным сделать?
- [ ] Текстурам (diffuse and texture) backpack.obj ID присваивается не по порядку при старте движка

### Рефлексия
- [ ] Добавить простую рефлексию (поля, размеры полей, оффсет поля) для отрисвоки гуя

### Платформы
- [ ] Сборка на macOS (приоритет ARM-чипы)